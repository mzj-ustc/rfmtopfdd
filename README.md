# rfmtopfdd
Code for some cases in the paper
For comparisons of the same total number of parameters but different numbers of blocks, you can run `rfmto-irdd3d.py`.
For comparisons of values ​​before and after binarization, you can run `case2calculate.ipynb`. 
For weight of IR analysis, you can run `anlysis_ir.py`.
